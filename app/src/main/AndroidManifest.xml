<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.adimarket">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!-- Queries for WhatsApp -->
    <queries>
        <package android:name="com.whatsapp" />
        <package android:name="com.whatsapp.w4b" />
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
    </queries>

    <application
        android:name=".AdiMarketApp"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher_round_custom"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher_round_custom"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.DayNight.NoActionBar">

        <!-- Getting Started Activity sebagai Launcher -->
        <activity
            android:name=".GettingStartedActivity"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Splash Activity -->
        <activity
            android:name=".SplashActivity"
            android:exported="false"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />

        <!-- Dashboard Activity -->
        <activity
            android:name=".DashboardActivity"
            android:exported="true" />

        <!-- Jual Kendaraan Activity -->
        <activity
            android:name=".JualKendaraanActivity"
            android:exported="false" />

        <!-- Seller Verification Activity -->
        <activity
            android:name=".SellerVerificationActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Beli Kendaraan Activity -->
        <activity
            android:name=".BeliKendaraanActivity"
            android:exported="true" />

        <!-- Admin Approval Activity -->
        <activity
            android:name=".AdminApprovalActivity"
            android:exported="false"
            android:label="Admin Approval" />

        <!-- Riwayat Transaksi Activity -->
        <activity
            android:name=".RiwayatTransaksiActivity"
            android:exported="false" />

        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <!-- AI Features Activities -->
        <activity android:name=".ImageRecognitionActivity" android:exported="false" />
        <activity android:name=".PricePredictionActivity" android:exported="false" />
        <activity android:name=".ChatbotActivity" android:exported="false" />
        <activity android:name=".OCRActivity" android:exported="false" />
        <activity android:name=".VoiceSearchActivity" android:exported="false" />
        <activity android:name=".SalesAnalysisActivity" android:exported="false" />
        <activity android:name=".NotifikasiActivity" android:exported="false" />

        <!-- Login, Register, Profil -->
        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegisterActivity" android:exported="false"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
        <activity android:name=".ForgotPasswordActivity" android:exported="false"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
        <activity android:name=".ProfilActivity" android:exported="false"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar" />
        <activity android:name=".OffersActivity" android:exported="false"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar" />

        <!-- Scan Capture Activity (CameraX Scanner) -->
        <activity
            android:name=".ScanCaptureActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />

        <!-- FileProvider untuk share file PDF -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_provider_paths" />
        </provider>

    </application>

</manifest>